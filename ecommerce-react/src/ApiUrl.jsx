import React from 'react'

function ApiUrl(props) {
  return (
    <div>
   const ImgUrl = `http://127.0.0.1:8090/api/files/${props.collectionId}/${props.id}/${props.image}`;
    </div>
  )
}

export default ApiUrl;